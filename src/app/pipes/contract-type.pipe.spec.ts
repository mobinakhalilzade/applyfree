import { ContractTypePipe } from './contract-type.pipe';

describe('ContractTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ContractTypePipe();
    expect(pipe).toBeTruthy();
  });
});
